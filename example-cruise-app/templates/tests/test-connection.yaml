apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-cruise-app.fullname" . }}-test-connection"
  labels:
    {{- include "example-cruise-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-cruise-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
